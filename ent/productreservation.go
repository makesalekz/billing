// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/makesalekz/billing/ent/enum"
	"github.com/makesalekz/billing/ent/invoice"
	"github.com/makesalekz/billing/ent/product"
	"github.com/makesalekz/billing/ent/productreservation"
)

// ProductReservation is the model entity for the ProductReservation schema.
type ProductReservation struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// References the product being reserved.
	ProductID int64 `json:"product_id,omitempty"`
	// References the invoice associated with the reservation.
	InvoiceID int64 `json:"invoice_id,omitempty"`
	// References the user who created the reservation.
	UserID int64 `json:"user_id,omitempty"`
	// The quantity of the product reserved.
	ReservedQuantity int64 `json:"reserved_quantity,omitempty"`
	// The status of the reservation.
	Status enum.ReservationStatus `json:"status,omitempty"`
	// The time until the reservation expires.
	ExpirationTime time.Time `json:"expiration_time,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ProductReservationQuery when eager-loading is set.
	Edges        ProductReservationEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ProductReservationEdges holds the relations/edges for other nodes in the graph.
type ProductReservationEdges struct {
	// Product holds the value of the product edge.
	Product *Product `json:"product,omitempty"`
	// Invoice holds the value of the invoice edge.
	Invoice *Invoice `json:"invoice,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ProductOrErr returns the Product value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ProductReservationEdges) ProductOrErr() (*Product, error) {
	if e.Product != nil {
		return e.Product, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: product.Label}
	}
	return nil, &NotLoadedError{edge: "product"}
}

// InvoiceOrErr returns the Invoice value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ProductReservationEdges) InvoiceOrErr() (*Invoice, error) {
	if e.Invoice != nil {
		return e.Invoice, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: invoice.Label}
	}
	return nil, &NotLoadedError{edge: "invoice"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ProductReservation) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case productreservation.FieldID, productreservation.FieldProductID, productreservation.FieldInvoiceID, productreservation.FieldUserID, productreservation.FieldReservedQuantity:
			values[i] = new(sql.NullInt64)
		case productreservation.FieldStatus:
			values[i] = new(sql.NullString)
		case productreservation.FieldCreatedAt, productreservation.FieldUpdatedAt, productreservation.FieldExpirationTime:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ProductReservation fields.
func (pr *ProductReservation) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case productreservation.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			pr.ID = int64(value.Int64)
		case productreservation.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				pr.CreatedAt = value.Time
			}
		case productreservation.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				pr.UpdatedAt = value.Time
			}
		case productreservation.FieldProductID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field product_id", values[i])
			} else if value.Valid {
				pr.ProductID = value.Int64
			}
		case productreservation.FieldInvoiceID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field invoice_id", values[i])
			} else if value.Valid {
				pr.InvoiceID = value.Int64
			}
		case productreservation.FieldUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				pr.UserID = value.Int64
			}
		case productreservation.FieldReservedQuantity:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field reserved_quantity", values[i])
			} else if value.Valid {
				pr.ReservedQuantity = value.Int64
			}
		case productreservation.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				pr.Status = enum.ReservationStatus(value.String)
			}
		case productreservation.FieldExpirationTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field expiration_time", values[i])
			} else if value.Valid {
				pr.ExpirationTime = value.Time
			}
		default:
			pr.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ProductReservation.
// This includes values selected through modifiers, order, etc.
func (pr *ProductReservation) Value(name string) (ent.Value, error) {
	return pr.selectValues.Get(name)
}

// QueryProduct queries the "product" edge of the ProductReservation entity.
func (pr *ProductReservation) QueryProduct() *ProductQuery {
	return NewProductReservationClient(pr.config).QueryProduct(pr)
}

// QueryInvoice queries the "invoice" edge of the ProductReservation entity.
func (pr *ProductReservation) QueryInvoice() *InvoiceQuery {
	return NewProductReservationClient(pr.config).QueryInvoice(pr)
}

// Update returns a builder for updating this ProductReservation.
// Note that you need to call ProductReservation.Unwrap() before calling this method if this ProductReservation
// was returned from a transaction, and the transaction was committed or rolled back.
func (pr *ProductReservation) Update() *ProductReservationUpdateOne {
	return NewProductReservationClient(pr.config).UpdateOne(pr)
}

// Unwrap unwraps the ProductReservation entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pr *ProductReservation) Unwrap() *ProductReservation {
	_tx, ok := pr.config.driver.(*txDriver)
	if !ok {
		panic("ent: ProductReservation is not a transactional entity")
	}
	pr.config.driver = _tx.drv
	return pr
}

// String implements the fmt.Stringer.
func (pr *ProductReservation) String() string {
	var builder strings.Builder
	builder.WriteString("ProductReservation(")
	builder.WriteString(fmt.Sprintf("id=%v, ", pr.ID))
	builder.WriteString("created_at=")
	builder.WriteString(pr.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(pr.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("product_id=")
	builder.WriteString(fmt.Sprintf("%v", pr.ProductID))
	builder.WriteString(", ")
	builder.WriteString("invoice_id=")
	builder.WriteString(fmt.Sprintf("%v", pr.InvoiceID))
	builder.WriteString(", ")
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", pr.UserID))
	builder.WriteString(", ")
	builder.WriteString("reserved_quantity=")
	builder.WriteString(fmt.Sprintf("%v", pr.ReservedQuantity))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", pr.Status))
	builder.WriteString(", ")
	builder.WriteString("expiration_time=")
	builder.WriteString(pr.ExpirationTime.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ProductReservations is a parsable slice of ProductReservation.
type ProductReservations []*ProductReservation

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"gitlab.calendaria.team/services/finance/invoices/ent/consumedstatus"
	"gitlab.calendaria.team/services/finance/invoices/ent/item"
	"gitlab.calendaria.team/services/finance/invoices/ent/predicate"
)

// ConsumedStatusQuery is the builder for querying ConsumedStatus entities.
type ConsumedStatusQuery struct {
	config
	ctx        *QueryContext
	order      []consumedstatus.OrderOption
	inters     []Interceptor
	predicates []predicate.ConsumedStatus
	withItem   *ItemQuery
	withFKs    bool
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ConsumedStatusQuery builder.
func (csq *ConsumedStatusQuery) Where(ps ...predicate.ConsumedStatus) *ConsumedStatusQuery {
	csq.predicates = append(csq.predicates, ps...)
	return csq
}

// Limit the number of records to be returned by this query.
func (csq *ConsumedStatusQuery) Limit(limit int) *ConsumedStatusQuery {
	csq.ctx.Limit = &limit
	return csq
}

// Offset to start from.
func (csq *ConsumedStatusQuery) Offset(offset int) *ConsumedStatusQuery {
	csq.ctx.Offset = &offset
	return csq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (csq *ConsumedStatusQuery) Unique(unique bool) *ConsumedStatusQuery {
	csq.ctx.Unique = &unique
	return csq
}

// Order specifies how the records should be ordered.
func (csq *ConsumedStatusQuery) Order(o ...consumedstatus.OrderOption) *ConsumedStatusQuery {
	csq.order = append(csq.order, o...)
	return csq
}

// QueryItem chains the current query on the "item" edge.
func (csq *ConsumedStatusQuery) QueryItem() *ItemQuery {
	query := (&ItemClient{config: csq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := csq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := csq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(consumedstatus.Table, consumedstatus.FieldID, selector),
			sqlgraph.To(item.Table, item.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, consumedstatus.ItemTable, consumedstatus.ItemColumn),
		)
		fromU = sqlgraph.SetNeighbors(csq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ConsumedStatus entity from the query.
// Returns a *NotFoundError when no ConsumedStatus was found.
func (csq *ConsumedStatusQuery) First(ctx context.Context) (*ConsumedStatus, error) {
	nodes, err := csq.Limit(1).All(setContextOp(ctx, csq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{consumedstatus.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (csq *ConsumedStatusQuery) FirstX(ctx context.Context) *ConsumedStatus {
	node, err := csq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ConsumedStatus ID from the query.
// Returns a *NotFoundError when no ConsumedStatus ID was found.
func (csq *ConsumedStatusQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = csq.Limit(1).IDs(setContextOp(ctx, csq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{consumedstatus.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (csq *ConsumedStatusQuery) FirstIDX(ctx context.Context) int64 {
	id, err := csq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ConsumedStatus entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ConsumedStatus entity is found.
// Returns a *NotFoundError when no ConsumedStatus entities are found.
func (csq *ConsumedStatusQuery) Only(ctx context.Context) (*ConsumedStatus, error) {
	nodes, err := csq.Limit(2).All(setContextOp(ctx, csq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{consumedstatus.Label}
	default:
		return nil, &NotSingularError{consumedstatus.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (csq *ConsumedStatusQuery) OnlyX(ctx context.Context) *ConsumedStatus {
	node, err := csq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ConsumedStatus ID in the query.
// Returns a *NotSingularError when more than one ConsumedStatus ID is found.
// Returns a *NotFoundError when no entities are found.
func (csq *ConsumedStatusQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = csq.Limit(2).IDs(setContextOp(ctx, csq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{consumedstatus.Label}
	default:
		err = &NotSingularError{consumedstatus.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (csq *ConsumedStatusQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := csq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ConsumedStatusSlice.
func (csq *ConsumedStatusQuery) All(ctx context.Context) ([]*ConsumedStatus, error) {
	ctx = setContextOp(ctx, csq.ctx, ent.OpQueryAll)
	if err := csq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ConsumedStatus, *ConsumedStatusQuery]()
	return withInterceptors[[]*ConsumedStatus](ctx, csq, qr, csq.inters)
}

// AllX is like All, but panics if an error occurs.
func (csq *ConsumedStatusQuery) AllX(ctx context.Context) []*ConsumedStatus {
	nodes, err := csq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ConsumedStatus IDs.
func (csq *ConsumedStatusQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if csq.ctx.Unique == nil && csq.path != nil {
		csq.Unique(true)
	}
	ctx = setContextOp(ctx, csq.ctx, ent.OpQueryIDs)
	if err = csq.Select(consumedstatus.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (csq *ConsumedStatusQuery) IDsX(ctx context.Context) []int64 {
	ids, err := csq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (csq *ConsumedStatusQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, csq.ctx, ent.OpQueryCount)
	if err := csq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, csq, querierCount[*ConsumedStatusQuery](), csq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (csq *ConsumedStatusQuery) CountX(ctx context.Context) int {
	count, err := csq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (csq *ConsumedStatusQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, csq.ctx, ent.OpQueryExist)
	switch _, err := csq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (csq *ConsumedStatusQuery) ExistX(ctx context.Context) bool {
	exist, err := csq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ConsumedStatusQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (csq *ConsumedStatusQuery) Clone() *ConsumedStatusQuery {
	if csq == nil {
		return nil
	}
	return &ConsumedStatusQuery{
		config:     csq.config,
		ctx:        csq.ctx.Clone(),
		order:      append([]consumedstatus.OrderOption{}, csq.order...),
		inters:     append([]Interceptor{}, csq.inters...),
		predicates: append([]predicate.ConsumedStatus{}, csq.predicates...),
		withItem:   csq.withItem.Clone(),
		// clone intermediate query.
		sql:       csq.sql.Clone(),
		path:      csq.path,
		modifiers: append([]func(*sql.Selector){}, csq.modifiers...),
	}
}

// WithItem tells the query-builder to eager-load the nodes that are connected to
// the "item" edge. The optional arguments are used to configure the query builder of the edge.
func (csq *ConsumedStatusQuery) WithItem(opts ...func(*ItemQuery)) *ConsumedStatusQuery {
	query := (&ItemClient{config: csq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	csq.withItem = query
	return csq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID int64 `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ConsumedStatus.Query().
//		GroupBy(consumedstatus.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (csq *ConsumedStatusQuery) GroupBy(field string, fields ...string) *ConsumedStatusGroupBy {
	csq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ConsumedStatusGroupBy{build: csq}
	grbuild.flds = &csq.ctx.Fields
	grbuild.label = consumedstatus.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID int64 `json:"user_id,omitempty"`
//	}
//
//	client.ConsumedStatus.Query().
//		Select(consumedstatus.FieldUserID).
//		Scan(ctx, &v)
func (csq *ConsumedStatusQuery) Select(fields ...string) *ConsumedStatusSelect {
	csq.ctx.Fields = append(csq.ctx.Fields, fields...)
	sbuild := &ConsumedStatusSelect{ConsumedStatusQuery: csq}
	sbuild.label = consumedstatus.Label
	sbuild.flds, sbuild.scan = &csq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ConsumedStatusSelect configured with the given aggregations.
func (csq *ConsumedStatusQuery) Aggregate(fns ...AggregateFunc) *ConsumedStatusSelect {
	return csq.Select().Aggregate(fns...)
}

func (csq *ConsumedStatusQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range csq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, csq); err != nil {
				return err
			}
		}
	}
	for _, f := range csq.ctx.Fields {
		if !consumedstatus.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if csq.path != nil {
		prev, err := csq.path(ctx)
		if err != nil {
			return err
		}
		csq.sql = prev
	}
	return nil
}

func (csq *ConsumedStatusQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ConsumedStatus, error) {
	var (
		nodes       = []*ConsumedStatus{}
		withFKs     = csq.withFKs
		_spec       = csq.querySpec()
		loadedTypes = [1]bool{
			csq.withItem != nil,
		}
	)
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, consumedstatus.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ConsumedStatus).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ConsumedStatus{config: csq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(csq.modifiers) > 0 {
		_spec.Modifiers = csq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, csq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := csq.withItem; query != nil {
		if err := csq.loadItem(ctx, query, nodes, nil,
			func(n *ConsumedStatus, e *Item) { n.Edges.Item = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (csq *ConsumedStatusQuery) loadItem(ctx context.Context, query *ItemQuery, nodes []*ConsumedStatus, init func(*ConsumedStatus), assign func(*ConsumedStatus, *Item)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*ConsumedStatus)
	for i := range nodes {
		fk := nodes[i].ItemID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(item.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "item_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (csq *ConsumedStatusQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := csq.querySpec()
	if len(csq.modifiers) > 0 {
		_spec.Modifiers = csq.modifiers
	}
	_spec.Node.Columns = csq.ctx.Fields
	if len(csq.ctx.Fields) > 0 {
		_spec.Unique = csq.ctx.Unique != nil && *csq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, csq.driver, _spec)
}

func (csq *ConsumedStatusQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(consumedstatus.Table, consumedstatus.Columns, sqlgraph.NewFieldSpec(consumedstatus.FieldID, field.TypeInt64))
	_spec.From = csq.sql
	if unique := csq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if csq.path != nil {
		_spec.Unique = true
	}
	if fields := csq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, consumedstatus.FieldID)
		for i := range fields {
			if fields[i] != consumedstatus.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if csq.withItem != nil {
			_spec.Node.AddColumnOnce(consumedstatus.FieldItemID)
		}
	}
	if ps := csq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := csq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := csq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := csq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (csq *ConsumedStatusQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(csq.driver.Dialect())
	t1 := builder.Table(consumedstatus.Table)
	columns := csq.ctx.Fields
	if len(columns) == 0 {
		columns = consumedstatus.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if csq.sql != nil {
		selector = csq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if csq.ctx.Unique != nil && *csq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range csq.modifiers {
		m(selector)
	}
	for _, p := range csq.predicates {
		p(selector)
	}
	for _, p := range csq.order {
		p(selector)
	}
	if offset := csq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := csq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (csq *ConsumedStatusQuery) Modify(modifiers ...func(s *sql.Selector)) *ConsumedStatusSelect {
	csq.modifiers = append(csq.modifiers, modifiers...)
	return csq.Select()
}

// ConsumedStatusGroupBy is the group-by builder for ConsumedStatus entities.
type ConsumedStatusGroupBy struct {
	selector
	build *ConsumedStatusQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (csgb *ConsumedStatusGroupBy) Aggregate(fns ...AggregateFunc) *ConsumedStatusGroupBy {
	csgb.fns = append(csgb.fns, fns...)
	return csgb
}

// Scan applies the selector query and scans the result into the given value.
func (csgb *ConsumedStatusGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, csgb.build.ctx, ent.OpQueryGroupBy)
	if err := csgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ConsumedStatusQuery, *ConsumedStatusGroupBy](ctx, csgb.build, csgb, csgb.build.inters, v)
}

func (csgb *ConsumedStatusGroupBy) sqlScan(ctx context.Context, root *ConsumedStatusQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(csgb.fns))
	for _, fn := range csgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*csgb.flds)+len(csgb.fns))
		for _, f := range *csgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*csgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := csgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ConsumedStatusSelect is the builder for selecting fields of ConsumedStatus entities.
type ConsumedStatusSelect struct {
	*ConsumedStatusQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (css *ConsumedStatusSelect) Aggregate(fns ...AggregateFunc) *ConsumedStatusSelect {
	css.fns = append(css.fns, fns...)
	return css
}

// Scan applies the selector query and scans the result into the given value.
func (css *ConsumedStatusSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, css.ctx, ent.OpQuerySelect)
	if err := css.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ConsumedStatusQuery, *ConsumedStatusSelect](ctx, css.ConsumedStatusQuery, css, css.inters, v)
}

func (css *ConsumedStatusSelect) sqlScan(ctx context.Context, root *ConsumedStatusQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(css.fns))
	for _, fn := range css.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*css.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := css.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (css *ConsumedStatusSelect) Modify(modifiers ...func(s *sql.Selector)) *ConsumedStatusSelect {
	css.modifiers = append(css.modifiers, modifiers...)
	return css
}

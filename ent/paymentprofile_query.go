// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"gitlab.calendaria.team/services/finance/billing/ent/invoice"
	"gitlab.calendaria.team/services/finance/billing/ent/paymentprofile"
	"gitlab.calendaria.team/services/finance/billing/ent/predicate"
)

// PaymentProfileQuery is the builder for querying PaymentProfile entities.
type PaymentProfileQuery struct {
	config
	ctx          *QueryContext
	order        []paymentprofile.OrderOption
	inters       []Interceptor
	predicates   []predicate.PaymentProfile
	withInvoices *InvoiceQuery
	modifiers    []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PaymentProfileQuery builder.
func (ppq *PaymentProfileQuery) Where(ps ...predicate.PaymentProfile) *PaymentProfileQuery {
	ppq.predicates = append(ppq.predicates, ps...)
	return ppq
}

// Limit the number of records to be returned by this query.
func (ppq *PaymentProfileQuery) Limit(limit int) *PaymentProfileQuery {
	ppq.ctx.Limit = &limit
	return ppq
}

// Offset to start from.
func (ppq *PaymentProfileQuery) Offset(offset int) *PaymentProfileQuery {
	ppq.ctx.Offset = &offset
	return ppq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ppq *PaymentProfileQuery) Unique(unique bool) *PaymentProfileQuery {
	ppq.ctx.Unique = &unique
	return ppq
}

// Order specifies how the records should be ordered.
func (ppq *PaymentProfileQuery) Order(o ...paymentprofile.OrderOption) *PaymentProfileQuery {
	ppq.order = append(ppq.order, o...)
	return ppq
}

// QueryInvoices chains the current query on the "invoices" edge.
func (ppq *PaymentProfileQuery) QueryInvoices() *InvoiceQuery {
	query := (&InvoiceClient{config: ppq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ppq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ppq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(paymentprofile.Table, paymentprofile.FieldID, selector),
			sqlgraph.To(invoice.Table, invoice.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, paymentprofile.InvoicesTable, paymentprofile.InvoicesColumn),
		)
		fromU = sqlgraph.SetNeighbors(ppq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PaymentProfile entity from the query.
// Returns a *NotFoundError when no PaymentProfile was found.
func (ppq *PaymentProfileQuery) First(ctx context.Context) (*PaymentProfile, error) {
	nodes, err := ppq.Limit(1).All(setContextOp(ctx, ppq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{paymentprofile.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ppq *PaymentProfileQuery) FirstX(ctx context.Context) *PaymentProfile {
	node, err := ppq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PaymentProfile ID from the query.
// Returns a *NotFoundError when no PaymentProfile ID was found.
func (ppq *PaymentProfileQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = ppq.Limit(1).IDs(setContextOp(ctx, ppq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{paymentprofile.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ppq *PaymentProfileQuery) FirstIDX(ctx context.Context) int64 {
	id, err := ppq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PaymentProfile entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PaymentProfile entity is found.
// Returns a *NotFoundError when no PaymentProfile entities are found.
func (ppq *PaymentProfileQuery) Only(ctx context.Context) (*PaymentProfile, error) {
	nodes, err := ppq.Limit(2).All(setContextOp(ctx, ppq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{paymentprofile.Label}
	default:
		return nil, &NotSingularError{paymentprofile.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ppq *PaymentProfileQuery) OnlyX(ctx context.Context) *PaymentProfile {
	node, err := ppq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PaymentProfile ID in the query.
// Returns a *NotSingularError when more than one PaymentProfile ID is found.
// Returns a *NotFoundError when no entities are found.
func (ppq *PaymentProfileQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = ppq.Limit(2).IDs(setContextOp(ctx, ppq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{paymentprofile.Label}
	default:
		err = &NotSingularError{paymentprofile.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ppq *PaymentProfileQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := ppq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PaymentProfiles.
func (ppq *PaymentProfileQuery) All(ctx context.Context) ([]*PaymentProfile, error) {
	ctx = setContextOp(ctx, ppq.ctx, ent.OpQueryAll)
	if err := ppq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PaymentProfile, *PaymentProfileQuery]()
	return withInterceptors[[]*PaymentProfile](ctx, ppq, qr, ppq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ppq *PaymentProfileQuery) AllX(ctx context.Context) []*PaymentProfile {
	nodes, err := ppq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PaymentProfile IDs.
func (ppq *PaymentProfileQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if ppq.ctx.Unique == nil && ppq.path != nil {
		ppq.Unique(true)
	}
	ctx = setContextOp(ctx, ppq.ctx, ent.OpQueryIDs)
	if err = ppq.Select(paymentprofile.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ppq *PaymentProfileQuery) IDsX(ctx context.Context) []int64 {
	ids, err := ppq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ppq *PaymentProfileQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ppq.ctx, ent.OpQueryCount)
	if err := ppq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ppq, querierCount[*PaymentProfileQuery](), ppq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ppq *PaymentProfileQuery) CountX(ctx context.Context) int {
	count, err := ppq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ppq *PaymentProfileQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ppq.ctx, ent.OpQueryExist)
	switch _, err := ppq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ppq *PaymentProfileQuery) ExistX(ctx context.Context) bool {
	exist, err := ppq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PaymentProfileQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ppq *PaymentProfileQuery) Clone() *PaymentProfileQuery {
	if ppq == nil {
		return nil
	}
	return &PaymentProfileQuery{
		config:       ppq.config,
		ctx:          ppq.ctx.Clone(),
		order:        append([]paymentprofile.OrderOption{}, ppq.order...),
		inters:       append([]Interceptor{}, ppq.inters...),
		predicates:   append([]predicate.PaymentProfile{}, ppq.predicates...),
		withInvoices: ppq.withInvoices.Clone(),
		// clone intermediate query.
		sql:       ppq.sql.Clone(),
		path:      ppq.path,
		modifiers: append([]func(*sql.Selector){}, ppq.modifiers...),
	}
}

// WithInvoices tells the query-builder to eager-load the nodes that are connected to
// the "invoices" edge. The optional arguments are used to configure the query builder of the edge.
func (ppq *PaymentProfileQuery) WithInvoices(opts ...func(*InvoiceQuery)) *PaymentProfileQuery {
	query := (&InvoiceClient{config: ppq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ppq.withInvoices = query
	return ppq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PaymentProfile.Query().
//		GroupBy(paymentprofile.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ppq *PaymentProfileQuery) GroupBy(field string, fields ...string) *PaymentProfileGroupBy {
	ppq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PaymentProfileGroupBy{build: ppq}
	grbuild.flds = &ppq.ctx.Fields
	grbuild.label = paymentprofile.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.PaymentProfile.Query().
//		Select(paymentprofile.FieldCreatedAt).
//		Scan(ctx, &v)
func (ppq *PaymentProfileQuery) Select(fields ...string) *PaymentProfileSelect {
	ppq.ctx.Fields = append(ppq.ctx.Fields, fields...)
	sbuild := &PaymentProfileSelect{PaymentProfileQuery: ppq}
	sbuild.label = paymentprofile.Label
	sbuild.flds, sbuild.scan = &ppq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PaymentProfileSelect configured with the given aggregations.
func (ppq *PaymentProfileQuery) Aggregate(fns ...AggregateFunc) *PaymentProfileSelect {
	return ppq.Select().Aggregate(fns...)
}

func (ppq *PaymentProfileQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ppq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ppq); err != nil {
				return err
			}
		}
	}
	for _, f := range ppq.ctx.Fields {
		if !paymentprofile.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ppq.path != nil {
		prev, err := ppq.path(ctx)
		if err != nil {
			return err
		}
		ppq.sql = prev
	}
	return nil
}

func (ppq *PaymentProfileQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PaymentProfile, error) {
	var (
		nodes       = []*PaymentProfile{}
		_spec       = ppq.querySpec()
		loadedTypes = [1]bool{
			ppq.withInvoices != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PaymentProfile).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PaymentProfile{config: ppq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(ppq.modifiers) > 0 {
		_spec.Modifiers = ppq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ppq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ppq.withInvoices; query != nil {
		if err := ppq.loadInvoices(ctx, query, nodes,
			func(n *PaymentProfile) { n.Edges.Invoices = []*Invoice{} },
			func(n *PaymentProfile, e *Invoice) { n.Edges.Invoices = append(n.Edges.Invoices, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ppq *PaymentProfileQuery) loadInvoices(ctx context.Context, query *InvoiceQuery, nodes []*PaymentProfile, init func(*PaymentProfile), assign func(*PaymentProfile, *Invoice)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*PaymentProfile)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(invoice.FieldPaymentProfileID)
	}
	query.Where(predicate.Invoice(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(paymentprofile.InvoicesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.PaymentProfileID
		if fk == nil {
			return fmt.Errorf(`foreign-key "payment_profile_id" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "payment_profile_id" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (ppq *PaymentProfileQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ppq.querySpec()
	if len(ppq.modifiers) > 0 {
		_spec.Modifiers = ppq.modifiers
	}
	_spec.Node.Columns = ppq.ctx.Fields
	if len(ppq.ctx.Fields) > 0 {
		_spec.Unique = ppq.ctx.Unique != nil && *ppq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ppq.driver, _spec)
}

func (ppq *PaymentProfileQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(paymentprofile.Table, paymentprofile.Columns, sqlgraph.NewFieldSpec(paymentprofile.FieldID, field.TypeInt64))
	_spec.From = ppq.sql
	if unique := ppq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ppq.path != nil {
		_spec.Unique = true
	}
	if fields := ppq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, paymentprofile.FieldID)
		for i := range fields {
			if fields[i] != paymentprofile.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ppq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ppq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ppq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ppq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ppq *PaymentProfileQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ppq.driver.Dialect())
	t1 := builder.Table(paymentprofile.Table)
	columns := ppq.ctx.Fields
	if len(columns) == 0 {
		columns = paymentprofile.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ppq.sql != nil {
		selector = ppq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ppq.ctx.Unique != nil && *ppq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range ppq.modifiers {
		m(selector)
	}
	for _, p := range ppq.predicates {
		p(selector)
	}
	for _, p := range ppq.order {
		p(selector)
	}
	if offset := ppq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ppq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ppq *PaymentProfileQuery) Modify(modifiers ...func(s *sql.Selector)) *PaymentProfileSelect {
	ppq.modifiers = append(ppq.modifiers, modifiers...)
	return ppq.Select()
}

// PaymentProfileGroupBy is the group-by builder for PaymentProfile entities.
type PaymentProfileGroupBy struct {
	selector
	build *PaymentProfileQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ppgb *PaymentProfileGroupBy) Aggregate(fns ...AggregateFunc) *PaymentProfileGroupBy {
	ppgb.fns = append(ppgb.fns, fns...)
	return ppgb
}

// Scan applies the selector query and scans the result into the given value.
func (ppgb *PaymentProfileGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ppgb.build.ctx, ent.OpQueryGroupBy)
	if err := ppgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PaymentProfileQuery, *PaymentProfileGroupBy](ctx, ppgb.build, ppgb, ppgb.build.inters, v)
}

func (ppgb *PaymentProfileGroupBy) sqlScan(ctx context.Context, root *PaymentProfileQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ppgb.fns))
	for _, fn := range ppgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ppgb.flds)+len(ppgb.fns))
		for _, f := range *ppgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ppgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ppgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PaymentProfileSelect is the builder for selecting fields of PaymentProfile entities.
type PaymentProfileSelect struct {
	*PaymentProfileQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pps *PaymentProfileSelect) Aggregate(fns ...AggregateFunc) *PaymentProfileSelect {
	pps.fns = append(pps.fns, fns...)
	return pps
}

// Scan applies the selector query and scans the result into the given value.
func (pps *PaymentProfileSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pps.ctx, ent.OpQuerySelect)
	if err := pps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PaymentProfileQuery, *PaymentProfileSelect](ctx, pps.PaymentProfileQuery, pps, pps.inters, v)
}

func (pps *PaymentProfileSelect) sqlScan(ctx context.Context, root *PaymentProfileQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pps.fns))
	for _, fn := range pps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (pps *PaymentProfileSelect) Modify(modifiers ...func(s *sql.Selector)) *PaymentProfileSelect {
	pps.modifiers = append(pps.modifiers, modifiers...)
	return pps
}
